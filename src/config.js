/* ═══════════════════════════════════════════════════════════════════════
   НАСТРОЙКИ ВХОДА

   ⚠⚠ ЧЕСТНО О ТОМ, ЧТО ЭТО ТАКОЕ.
   Бэкенда у приложения нет: это статические файлы на GitHub Pages. Значит
   проверка пароля происходит В БРАУЗЕРЕ, и обойти её может любой, кто
   откроет исходники страницы. Данные лежат в репозитории открыто — по
   решению владельца от 29.08.2026 их не скрывают.

   Поэтому вход здесь — ЗАСЛОН ОТ СЛУЧАЙНОГО ГОСТЯ, а не защита. Он делает
   ровно одно: телефон, оставленный на столе разблокированным, не показывает
   диагнозы первому, кто его взял. Всё. Называть это защитой персональных
   данных было бы неправдой, и приложение так не называет — на самом экране
   входа стоит строка про открытые данные.

   ✅ SYS-6 (Д-32, 29.08.2026, вечер): настоящее закрытие ВКЛЮЧЕНО.
   Данные шифруются на машине владельца (scripts/encrypt-data.mjs,
   AES-256-GCM), в публичный репозиторий уезжает только health.enc.json.
   Ключ выводится из этой же фразы (соль + ':data') после верного входа и
   нигде не хранится. Утреннее решение «мягкий гейт» отменено в тот же
   день — открытый JSON в публичном репозитории делал пароль декорацией.
   ⚠ Граница честности теперь другая: стойкость равна стойкости фразы,
   короткая фраза перебирается по словарю офлайн. Фраза должна быть длинной.
   ⚠ Смена пароля = два шага: новый хеш сюда + пересборка .enc той же фразой
   (KONTUR_PASS='фраза' npm run data:encrypt). Разойдутся — экран честно
   скажет, что пароль принят, а данные собраны другой фразой.

   ── КАК ПОМЕНЯТЬ ПАРОЛЬ ────────────────────────────────────────────────
   1. Придумать фразу.
   2. Посчитать её хеш тем же способом, каким его считает приложение:

        node scripts/passcode.mjs "новая фраза"

   3. Вставить полученную строку в PASSCODE_HASH ниже, пересобрать, запушить.

   ⚠ Сам пароль в коде НЕ хранится — только производная от него. Подобрать
     фразу по хешу перебором мешают соль и 120 000 итераций PBKDF2: это не
     делает гейт стойким (см. выше), но снимает самый дешёвый способ —
     сверку с готовой таблицей популярных паролей.
   ═══════════════════════════════════════════════════════════════════════ */

/** Логин — фиксирован и не редактируется, как в «Бумбастике» (boom-cmd).
 *  Поле показывает контекст входа, а не спрашивает его: во входе участвует
 *  только пароль. Пустое редактируемое поле обещало бы, что от логина
 *  что-то зависит, — обещание ложное. */
export const LOGIN = 'b00mbastic'

/** Соль. Постоянная, в коде — иначе её негде хранить без бэкенда.
 *  Её задача не в секретности, а в том, чтобы хеш этого приложения не
 *  совпадал с хешем той же фразы где-нибудь ещё. */
export const PASSCODE_SALT = 'kontur-zdorovya-2026'

/** Число итераций PBKDF2-SHA256. Держать синхронно со scripts/passcode.mjs. */
export const PASSCODE_ITERATIONS = 120000

/** PBKDF2(пароль, соль, 120000, SHA-256) → 32 байта в hex.
 *
 *  ⚠ ЗДЕСЬ ХЕШ ДЕЙСТВУЮЩЕЙ ФРАЗЫ ВЛАДЕЛЬЦА, а не заглушки: пароль сменён
 *  29.08.2026 (`npm run passcode` + пересборка .enc). Прежняя строка
 *  комментария — «пароль по умолчанию: kontur-2026» — осталась от первой
 *  сборки и вводила в заблуждение: 29.08 этот хеш был скопирован отсюда в
 *  репозиторий витрины «Успею» именно потому, что читался как заглушка.
 *  Поймано дымом витрины, откачено там же.
 *  ⛔ Отсюда следует не «не публиковать хеш дважды» (это ничего не меняет:
 *  соль и итерации те же, перебор стоит столько же), а простое:
 *  **хеш не копируют, его считают** — `npm run passcode "фраза"`. Копирование
 *  переносит не фразу, а чужое утверждение о том, какая она. */
export const PASSCODE_HASH =
  '7fe0855720c14b4c5b8c2e01507c8a867ad42f20ecbd4098a0f5f4879e474e70'

/** Срок жизни сессии. Час без действий — и фраза спрашивается снова.
 *  Значение и логика перенесены из boom-cmd/src/composables/useAccessKey.js. */
export const SESSION_TTL_MS = 60 * 60 * 1000

/** Ключ хранилища недельных отметок. Меняя его, ты обнуляешь историю опросов
 *  на устройстве — в мастере контура она при этом остаётся. */
export const WEEKLY_STORAGE_KEY = 'kontur.weekly.v1'

/** Ключ хранилища отметок химического слоя (SYS-10, Д-33). Отметки живут
 *  здесь до переноса в meds/intake_2026.md; смена ключа обнуляет их на
 *  устройстве, в мастере контура они при этом остаются. */
export const INTAKE_STORAGE_KEY = 'kontur.intake.v1'

/** Ключ снимка последнего показанного «Заряда» (для дельты «против прошлой
 *  недели» и для замка закрытого периода — см. chargeModel.js). */
export const CHARGE_STORAGE_KEY = 'kontur.charge.v1'
